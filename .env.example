# Add this to your .env file for better configuration management
VITE_WEBSITE_DOMAIN=https://hash-by-m-yousaf.vercel.app

# Note: Service workers can't access VITE_ environment variables, 
# so you'll need to update the domain in firebase-messaging-sw.js manually
# when deploying to different environments
